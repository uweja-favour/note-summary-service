spring:
  threads:
    virtual:
      enabled: true

  data:
    mongodb:
      uri: mongodb+srv://dbUser:Password123Password@cluster0.fiw5cvi.mongodb.net/note_summary_service_db?retryWrites=true&w=majority&appName=Cluster0

  webflux:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

  codec:
    max-in-memory-size: 500MB

transaction:
  default-timeout: 30s

server:
  port: ${PORT:8082}

# -----------------------------------------
# OPENAI CONFIG (Sensitive - should be env var in real systems)
# -----------------------------------------

anthropic:
  api:
    key: ${CLAUDE_API_KEY}

openai:
  api:
    key: ${GPT_API_KEY}
    
# -----------------------------------------
# SECURITY OVERRIDES (DEV ONLY)
# -----------------------------------------
security:
  auth:
    enabled: true

  jwt:
    accessSecret: asdfsdfarwegrsKLSAHAKBIUUIBuseweDOIy8b_)@#r43243234sadfew23fsxcasdfq32refwasegsade234ew
    refreshSecret: asdijfpeiwrAW3OSI9823JKKSokls0(U2w30(*IUNskajfwke2939sdf9aww9edsafds3wrwaeasdfa34sewe
    access-token:
      expiration-ms: 900000
    refresh-token:
      expiration-ms: 2592000000
